// File: components/checkout/LoginStep.jsx
import React from "react";

const LoginStep = () => {
  return (
    <div>
      <h2 className="text-lg font-semibold mb-4">Login</h2>
      <p>Assume user is already logged in or handled by parent auth flow.</p>
    </div>
  );
};

export default LoginStep;
